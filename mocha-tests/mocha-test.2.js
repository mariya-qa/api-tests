const fetch = require("node-fetch");

describe('API test', () => {
    const url = "http://todo-app-barkend.herokuapp.com";

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })

    it('fetches Todo items - GET', async () => {
        const rawResponse = await fetch(url + "/todos");
        const produtos = await rawResponse.json();
        console.log(produtos);
    })
})

